<beans
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:camel="http://camel.apache.org/schema/spring"
	xsi:schemaLocation="
		http://camel.apache.org/schema/spring https://camel.apache.org/schema/spring/camel-spring.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd"
>

	<bean id="propertyPlaceholderConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE" />
		<property name="searchSystemEnvironment" value="true" />
	</bean>

	<bean id="exchangeFormatter" class="net.maxf.apache.camel.FilteringExchangeFormatter">
		<property name="showExchangeId" value="true" />
		<property name="showProperties" value="true" />
		<property name="showAllProperties" value="true" />
		<property name="showVariables" value="true" />
		<property name="showHeaders" value="true" />
		<property name="showBodyType" value="false" />
		<property name="showBody" value="false" />
		<property name="showException" value="true" />
		<property name="showStackTrace" value="true" />

		<!-- Never trace credentials -->
		<property name="keyFilterPattern" value="(?i)^(.*authorization|cookie|set-cookie)$" />
		<!-- Full dump on the first trace of an exchange, only changes after that. On by default, override with -DDELTA_MODE=false -->
		<property name="deltaMode" value="${DELTA_MODE:true}" />
		<!-- Log "Unchanged" traces too. Off by default, override with -DTRACE_UNCHANGED=true -->
		<property name="traceUnchanged" value="${TRACE_UNCHANGED:false}" />
		<!-- Exchange property holding the last traced state -->
		<property name="snapshotProperty" value="net.maxf.traceDeltaSnapshot" />
		<!-- Collapse multiline values and stack traces into a single log line -->
		<property name="lineSeparator" value=" | " />
	</bean>

	<!-- FilteringTracer drops traces the formatter skipped, e.g. unchanged ones -->
	<bean id="tracer" class="net.maxf.apache.camel.FilteringTracer">
		<property name="exchangeFormatter" ref="exchangeFormatter" />
	</bean>

	<camelContext xmlns="http://camel.apache.org/schema/spring" trace="true">

		<route id="sample">
			<from uri="timer:sample?repeatCount=1" />
			<setHeader name="Authorization"><constant>Bearer never-traced</constant></setHeader>
			<setHeader name="stage"><constant>received</constant></setHeader>
			<setHeader name="note"><constant>first line&#10;second line</constant></setHeader>
			<setVariable name="attempt"><constant>1</constant></setVariable>
			<setBody><constant>a,b,c</constant></setBody>
			<split>
				<tokenize token="," />
				<setHeader name="item"><simple>${body}</simple></setHeader>
				<setBody><simple>${body.toUpperCase()}</simple></setBody>
			</split>
			<setHeader name="stage"><constant>done</constant></setHeader>
			<removeHeader name="note" />
			<log message="Processed ${body}" />
		</route>

	</camelContext>

</beans>
